#pragma once

void setupLighting();       // call once at startup
void applyLighting();       // call each frame
void applyCabinLighting();  // enable warm cabin light
void disableCabinLighting();// disable cabin light
